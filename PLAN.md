# Makina — 実装計画

> **Makina** — メッシュを持たないソリッドモデラー。
> CSG オペレータツリーを SDF として GPU で直接評価し、ジオメトリから導いたフィールドで
> リアルタイムに経年変化を与える。MitiruEngine の姉妹プロジェクト。

**状態**: Phase S / 0 / 1 / 2 / 4 / 5 完了、7 はコア側完了。残りは Phase 3（アプリと操作性）・6（エンジン統合）・8（Vulkan）。
**最終更新**: 2026-08-10

---

## 0. この文書の使い方

- **§2 の「完成の定義」が唯一のゴール**。ここに書いていないものは作らない。
- **§4 の決定記録は、覆すときに必ずこの文書を更新する。** 口頭で覆すと破綻する。
- **§6 のリスク登録簿にある「早期検証」は、順序を入れ替えない。** 破綻を前倒しで発見するために置いてある。
- 各フェーズには **完了条件（Exit Criteria）** がある。満たさずに次へ進まない。

---

## 1. 背景と前提

### 1.1 出自

`D:\sandbox\Grasp3D` — Java + JOGL + POV-Ray の教育用 3DCG モデラー（92ファイル / 約28,000行）。
CSG オペレータツリーでモデルを構成する。直接メッシュ編集を持たない。

これを C++ に移植し、DX12 でリアルタイム描画し、TA 職として評価される付加価値を載せる。

### 1.2 移植対象の実態

| 区分 | 規模 | 移植 |
|---|---|---|
| Swing UI / ヘルプ / i18n / コラボ通信 | 約 14,000 行 | **しない**（CEF/HTML で作り直し） |
| コアドメイン | 約 10〜12,000 行 | **する** |

移植するコア：

| Java | 内容 | 行数 |
|---|---|---|
| `SceneSdf.java` | CSGツリーの符号付き距離場評価器 | 522 |
| `CsgBsp.java` | csg.js 由来の BSP ブーリアン | — |
| `CsgTess.java` | プリミティブのテッセレーション | 344 |
| `SceneMeasure.java` | gap / overlap / floating / symmetry の数値検証 | 272 |
| **`PatchSolid.java`** | **厚みゼロの Disc/Triangle を CSG に入れるためのルール**（サイズの 2%、下限 1e-4 の厚みを与え、面はスラブ中央に置く）。⚠ **落とすと CSG が壊れる**（[CSG_NODE.md](docs/CSG_NODE.md) §4.3） | — |
| `SceneBounds` / `SceneMatrix` / `SceneAlign` | 境界・行列・相対配置 | — |
| プリミティブ群 | Box / Sphere / Cylinder / Cone / Torus / Plane / Disc / Triangle / SuperEllipsoidBox | — |
| `PovCamera` / `PovCutterStream` | POV-Ray 出力 | — |

> **重要**: `MyGLEventListener.java` は GL2 固定機能パイプライン（OpenGL 1.x 相当）。
> **移植すべきレンダラコードは存在しない。** 描画は完全新規。

### 1.3 MitiruEngine の実態（調査済み）

- v0.30.0 / C++20 / CMake 3.21+ / Windows 10-11 x64 専用
- `include/mitiru/` に 40 サブシステム、`include/` + `src/` で 726 ファイル
- **`gfx/`**: RHI 抽象（`IDevice` / `ICommandList` / `IBuffer` / `IPipeline` / `ISwapChain` / `IShader` / `ITexture` / `IRenderTarget` / `IDescriptorHeap` / `IGpuFence`）
  - バックエンド: **dx12 (3,808行)** / **vulkan (3,064行)** / dx11 / opengl / webgl / webgpu / null
  - ⚠ **`ICommandList` に `Dispatch` が無い。コンピュートは抽象越しに書けない。**
  - `IShader.hpp` に `ShaderStage::Compute` の enum 値だけ存在
- **`render/`**: `GBuffer` / `DeferredPipeline` / `ClusteredLighting` / `GlobalIllumination` / `TAAEffect` / **`SubsurfaceScatter`** / `ShadowMap` / `RenderGraph` / `PostProcess` / NPR・Toon 系
  - ⚠ **`Renderer3D_DX12.hpp` は `<d3d12.h>` を直 include。gfx 抽象を経由していない。**
  - ⚠ **`render/sdf/` は SDF フォント**（`SdfFontAtlas` 等）。ジオメトリ SDF は別の場所に置く。
- **CEF (Chromium Embedded Framework)**: UI / HUD を HTML / CSS で記述
- **FLAT_POD**: ゲーム状態は 1 個の `trivially_copyable` な struct（`GameMemory`）。
  ポインタ / `std::vector` / `std::string` 禁止。`FixedVec<T,N>` / `FixedString<N>` を使う。
  → 巻き戻し・録画再生・AI 構造化観測・反実仮想がここから生えている
- **`MITIRU_REFLECT`**: フィールドを宣言すると `GameMemory` が構造化 JSON になる
- **AI HTTP API**: `MITIRU_AI=1 mitiru run` → `127.0.0.1:8090/api/ai/state`、`/api/ai/diff`
- **docs/SCOPE.md（canonical）**: 「1ツール = 1関心事 = 1ウィンドウ」「**GUI editor は提供しない**」
  「authoring は全て code」「inspector は読み取り専用」「CLI が一級市民」

### 1.4 実行環境

- CPU: AMD Ryzen 7 5825U
- GPU: **Radeon 統合 GPU（RDNA2）/ VRAM 1GB**
- OS: Windows 11 Home
- ツールチェイン: Visual Studio 2026（VS18、v145）、CMake は VS 同梱

> **VRAM 1GB が全ての性能判断の前提。** メモリ重い設計（大量テクスチャ、高解像度3Dボリューム）は取れない。
> 逆にレイマーチは演算重・メモリ軽なので、この環境に適合する。

---

## 2. 完成の定義

**以下 5 点が揃った時点で「完成」。** これ以外は作らない。

| # | 成果物 | 検証方法 |
|---|---|---|
| **D1** | **ヒーローアセット 1 体**（摩耗した機械部品）が Makina 上でリアルタイム描画される | 目視 + FPS 計測 |
| **D2** | **30 秒のデモ録画**：ブーリアンノードをドラッグすると、エッジ摩耗・窪みの汚れ・上向き面の埃が**追従して流れ直す** | 録画ファイル |
| **D3** | **同じモデルが MitiruEngine のシーン上で動く**（ゲーム内オブジェクトとして） | 動作するサンプル |
| **D4** | **AI がツリーを編集するデモ**：自然言語の指示 → bridge API → ツリー変更 → 画面反映 | 録画 + ログ |
| **D5** | **3 系統クロスチェックのレポート**：BSP メッシュ / SDF レイマーチ / POV-Ray が同一形状を出すことの数値的確認 | レポート + CI |

### 2.1 MVP ライン（時間切れ時の切り捨て順）

**D1 + D2 まで到達していれば、単体で完成したポートフォリオになる。**
以降は D5 → D3 → D4 の順で価値が高い（D5 は既存資産の再利用なので安い）。

- **必須**: Phase S / 0 / 1 / 2 / 3 / 4 → D1, D2
- **強く推奨**: Phase 5 → D5（安い）
- **余力があれば**: Phase 6 → D3、Phase 7 → D4
- **完全に余力があれば**: Phase 8（Vulkan 引き上げ）

---

## 3. リポジトリ構成

```
makina-core/          独立リポジトリ。ヘッドレス、GPU 非依存、依存ゼロ
  include/makina/
    Node.hpp            CsgNode（flat POD）
    Tree.hpp            オーサリングツリー
    Sdf.hpp             ★ C++/HLSL 共用のプリミティブ SDF 定義
    Eval.hpp            CPU 評価器（SceneSdf 移植）
    Flatten.hpp         オーサリングツリー → 評価プログラム
    Bounds.hpp          AABB（SceneBounds 移植）
    Measure.hpp         gap/overlap/floating/symmetry（SceneMeasure 移植）
    Bsp.hpp             BSP ブーリアン（CsgBsp 移植、検証用）
    Serialize.hpp       JSON 入出力
    PovExport.hpp       POV-Ray 出力
  tests/

MitiruEngine/         既存。SCOPE.md を変更しない
  include/mitiru/render/csg/     ★ 新規（render/sdf はフォント用なので使わない）
    CsgRaymarchPass.hpp
    GeometryFields.hpp
  shaders/csg/                   ★ 新規（HLSL）

Makina/               新規リポジトリ。モデラー本体（別製品）
  src/                C++（ツリー編集、bridge、描画呼び出し）
  web/                CEF UI（HTML/CSS/JS、ノードエディタ含む）
```

### 3.1 依存方向（厳守）

```
makina-core  ←  MitiruEngine
      ↑              ↑
      └──── Makina ──┘
```

- `makina-core` は**何にも依存しない**（標準ライブラリのみ）。GPU も エンジンも知らない。
- `MitiruEngine` は `makina-core` に依存してよい（SDF 評価をランタイムで使うため）。
- **逆向きの依存を作らない。** 作った瞬間にテストが回らなくなる。

---

## 4. 設計上の確定事項（決定記録）

### D-01: モデルは 2 つの表現を持つ

| | オーサリングツリー | 評価プログラム |
|---|---|---|
| 置き場所 | `GameMemory`（FLAT POD） | `GameMemory` の外（DLL 内 static） |
| 構造 | Grasp3D 同様、`Translate`/`Rotate`/`Scale` を独立ノードとして持つ | プリミティブとブーリアンのみ。**トランスフォームは各プリミティブに焼き込み済み** |
| 順序 | 木構造（添字で親子） | **後置記法（RPN）の線形列** |
| 目的 | 設計意図の保存 / 人間と AI の可読性 / Undo / 差分 | GPU 評価の効率 |
| 生成 | ユーザー・AI が編集 | 変更時に `Flatten.hpp` が再生成 |

**理由**:
- GPU は再帰できない。木の後順走査は明示スタックが要る。**RPN に落とせば固定長スタック（深さ16程度）で足りる。**
- トランスフォームノードは「降りるときに点を変換」する。RPN の「昇るときに値を合成」と噛み合わない。
  → **CPU 側で各プリミティブに world→local 変換を焼き込む**ことで、GPU はプリミティブとブーリアンだけを見る。
- オーサリングツリーを意味のある形（名前付き `Difference`）で保つことが、本プロジェクトの主張そのもの。
  最適化のために意味を壊さない。

**FLAT_POD.md との整合**: 「観測ログのような非 gameplay 状態は `GameMemory` の外に置く」に従う。
評価プログラムは導出物なので `GameMemory` に入れない。

### D-02: `CsgNode` は flat POD、トランスフォームは別ノード【Task #2 で確定】

詳細と根拠は [CSG_NODE.md](docs/CSG_NODE.md)。

```cpp
struct CsgNode {
    std::uint32_t id;             // 不変 ID（追加削除でずれない）
    std::uint8_t  op;
    std::uint8_t  materialId;
    std::uint16_t flags;          // Rotate の軸 / Cone の Open / patch 判定
    std::uint16_t firstChild;     // ポインタではなく添字
    std::uint16_t childCount;     // n 項。Difference は [0] が本体、[1..] が刃
    std::uint16_t nameId;         // 名前テーブルへの添字（設計意図の保存）
    std::uint16_t _pad;
    float         params[12];     // 最大は Triangle の 10 個
};                                // ヘッダ 16 B + params 48 B = 64 バイト
```

⚠ **当初案の `params[8]` は否決。** Grasp3D の実パラメータを数え上げた結果、
**`Cone` が 9 個、`Triangle` が 10 個**必要だった（CSG_NODE.md §1）。
`flags` を分けたのは、`Rotate` の軸のような離散値を float に入れて
後で `== 1.0f` 比較する事故を防ぐため。

**サイズ試算**（巻き戻しリングバッファの容量に直結）:

**実測値**（`makina-core/tests/roundtrip.cpp` が毎回検証する）:

| 項目 | 実測 |
|---|---|
| `sizeof(CsgNode)` | **64 B** |
| `sizeof(Material)` | **40 B** |
| **`sizeof(Scene)`**（256 ノード / 64 マテリアル / 名前 32 字） | **27,152 B = 26.5 KB** |
| **巻き戻し 10 秒 @60fps** | **15.5 MB**（予算 32 MB） |

→ **予算の半分以下。** ノード上限 256 も妥当（Phase S で 100 ノード 63 fps 実測、
実データ最大の `pettobotoru.gsf` が 87 ノード）。

> この数値は `static_assert` とテストに組み込んである。**型が太ったらビルドかテストで落ちる。**

> トランスフォームを各ノードに 4x3 行列で inline すると 1 ノード 100 B を超え、
> 巻き戻しが 30 MB に迫る。**inline しない**理由はこれ。

### D-03: シェーダは HLSL + DXC の一本化

- **HLSL で書く。** DXC は `-spirv` で SPIR-V を吐けるので、Phase 8（Vulkan）でシェーダを書き直さない。
- **SDF プリミティブ関数は `makina-core/include/makina/Sdf.hpp` に 1 回だけ書く。**
  型エイリアス（`float3` 等）をマクロで吸収し、**C++ と HLSL の両方から同じソースを include する。**
  → CPU 評価器と GPU 評価器が定義上ズレない。§7 の検証戦略の土台。

### D-04: GPU 評価は**コード生成**を主、インタプリタを従【Phase S で確定・当初案から反転】

- CPU 側でツリーからシーン専用の HLSL `evalCsg` を生成する。スタックは生成時に消化されて
  **SSA 値**になり、変換行列は**即値**になり、分岐ディスパッチが消える。
- **実測 4.0〜5.0 倍**（[SPIKE_PERF.md](docs/SPIKE_PERF.md) §6）。
  50 ノード / AO / 720p が **34.94 ms → 7.37 ms**。出力画像はインタプリタ版と実質同一（最大差 1/255）。
- インタプリタ版も残す。**ドラッグ中の即応性**を担保する従の経路。

> **当初「コード生成はシェーダ再コンパイルを伴うので不可」と判断したのは誤りだった。**
> 再コンパイルが必要な場面を**構造変更時に限定できる**ことを見落としていた。
> パラメータのドラッグは木の構造を変えないので、構造だけを特殊化してパラメータを
> バッファから読めば、再コンパイルなしで済む可能性がある（Phase 2 冒頭で測る）。

- コンパイル遅延は **92〜232 ms**（ノード数に依存）。毎フレーム再生成は不可能。

**Task #8 の結果により、当初想定より穏やかな構成で解決する**（[SPIKE_PERF.md](docs/SPIKE_PERF.md) §6.5）：

| 版 | 速度 | 再コンパイルが要る場面 |
|---|---|---|
| **構造のみ特殊化**（寸法・変換はバッファ読み） | インタプリタ比 **2.5 倍** | **木の構造を変えたときだけ** |
| 完全リテラル | インタプリタ比 **4.9 倍** | パラメータ変更でも必要 |

4.9 倍の内訳は **構造特殊化 2.5 倍 × 定数畳み込み 2.0 倍**。

→ **常用は「構造のみ」。パラメータのドラッグは木の構造を変えないので再コンパイルが起きない。**
当初の「ドラッグ中に引っかかる」懸念はこれで消える。
**リテラル版は静止時の仕上げ描画**として、アイドル時にコンパイルして差し替える。
インタプリタは構造変更直後の穴埋めとして残す。

### D-05: Makina は MitiruEngine の一部にしない（別製品）

- `docs/SCOPE.md` は canonical であり「GUI editor は提供しない」と明記している。
- Makina は GUI オーサリングツールなので、エンジンに入れると SCOPE を自ら折ることになる。
- **エンジンは「エディタを提供しない」を守る。Makina がエディタとして外に立つ。**
- SCOPE.md の規律の強さ自体がポートフォリオの資産。薄めない。

### D-06: Makina の UI は CEF / HTML（ImGui は使わない）

- エンジンが CEF を持っており、HTML/CSS の UI が差別化要素。二重投資しない。
- **ノードベースのマテリアルエディタは HTML/CSS/JS で作る。** ImGui のノードエディタより工数が桁で軽い。
- bridge は signal-only（エンジンの規約）。JS → C++ は操作イベント、C++ → JS は状態 push のみ。
  **ツリーの正は常に C++ 側**（= `GameMemory`）。JS に状態を持たせない。

### D-07: 入力口は 3 つ、対象は 1 つ

同じオーサリングツリーを、3 つの経路が編集する。

| 経路 | 用途 | ポジション |
|---|---|---|
| GUI（CEF/HTML） | アーティストが触る | **TA としての訴求はここ** |
| コード / CLI | 再現性・バージョン管理・バッチ | エンジンの code-first 思想と整合 |
| AI（bridge API） | 自然言語からの編集 | 新テーマ |

### D-10: 操作性は Maya 系を既定とし、Blender 系をプリセットで切り替える

**背景**: Grasp3D は教育用途を優先した結果、操作性に不便が多い。実際、直近のコミットはこの領域に集中している。

```
dca118a  fix: カメラがパンした先を軸に回らない / パン量が距離で変わらない
ab9e3b7  fix: 数値欄の ↑↓ が別の部品へ飛ぶ問題 (値の増減に変更)
5054f42  feat: 光の x/y/z/w をホイールと矢印キーで動かせるように
```

**個別の不便を個別に直すのではなく、確立された規約を丸ごと採用する。**

**既定は Maya 系**。理由：

- **国内ゲーム業界は Maya が支配的**。就活で見せる相手の筋肉記憶に合わせる
- TA 職はアーティストとの橋渡しであり、**アーティストの手癖に合わせられること自体が評価対象**
- 「独自の操作系を発明しなかった」判断は、TA として正しい判断である

**Blender 系はプリセットで切り替え可能にする。** キーマップを設定ファイル（JSON）に外出しし、
ハードコードしない。※ `MitiruEngine/docs/INPUT_REBIND_RECIPE.md` が存在するため、
リバインド機構を流用できる可能性がある（Phase 3 着手時に要確認）。

**これは装飾ではなく成果物である。** TA のポートフォリオにおいて「アーティストが使いやすいか」は
技術力と同格の評価軸。§5 Phase 3 のチェックリストを満たすことを完了条件に含める。

### D-12: 厚みゼロ面は CSG のオペランドにしない（`PatchSolid` を移植しない）

`Disc` と `Triangle` は**面**であって内外を持たない。Grasp3D の `PatchSolid` は、
CSG 配下の面に**サイズの 2%** の厚みを暗黙に与えてこれを回避している。**移植しない。**

**理由 1：どのツールもやっていない。**

| ツール | 厚みゼロ面の CSG |
|---|---|
| POV-Ray | **拒否**（`PatchSolid.java` 自身が引用: `"Patch objects not allowed in intersection."`） |
| OpenSCAD | 2D は `linear_extrude` で立体化するまで 3D CSG に混ぜられない |
| B-rep カーネル（Parasolid / ACIS / OCCT） | ブーリアンは閉じた多様体を要求 |
| Blender | boolean modifier は manifold 前提 |
| SDF 系（libfive 等） | 面は符号なし距離しか持たず `max(a,-b)` が無意味 |

**理由 2：実装が定義されていない。** 厚みが**プリミティブのサイズに比例**する
（`RATIO = 0.02`）ため、**`Scale` を掛けると差分の深さが変わる**。近似ではなく未定義。

**理由 3：本プロジェクトの主張と矛盾する。** 「壊れたアセットが構造的に作れない」は
「閉じた立体の集合演算しか表現できない」ことに立脚している。
**非立体をオペランドに認めた瞬間に自壊する。**

**決定**：

- `Disc` / `Triangle` は **v1 では表示専用**。CSG のオペランドになれない
- CSG 配下に置かれていたら、**インポート時にエラーとして報告する**（黙って何かしない）
- `Plane` は**半空間＝無限の立体**で内部を持つので **CSG に入れてよい**。対象外ではない
- 将来必要になれば、OpenSCAD 流に**明示的な厚み付け操作**として足す。暗黙にサイズから決めない

**既存資産への影響はゼロ。** `Disc` / `Triangle` は Grasp3D 同梱の 4 つの `.gsf` に
1 個も出現しない（実測）。

### D-11: Grasp3D の不合理は理想形で実装する。ただし 2 種類に分けて扱う

移植は忠実さが目的ではなく、**動くものを作るのが目的**。Grasp3D 側の不合理をそのまま
持ち込む理由はない。ただし Phase 1 の合格条件は「Java 版と一致」なので、
無条件に理想化すると**検証基盤そのものが消える**。よって変更を 2 種類に分ける。

| | 内容 | 扱い |
|---|---|---|
| **(A) 幾何的出力が変わらない整理** | 死にフィールドの削除、名前の訂正、データ表現の変更 | **迷わず理想形にする。** 一致検証は緑のまま通るはずで、**通ることが「出力を変えていない」証明になる** |
| **(B) 出力が変わる変更** | 距離関数の式、CSG の意味論、境界条件の扱い | **実装してよいが、変えたことを [PORT_STATUS.md](docs/PORT_STATUS.md) に明記し、一致検証から意図的に除外する。** 黙って変えない |

**実施例（すべて A）**：

- `Cone` の 9 パラメータ → **`radius` / `height` の 2 個**。
  `Cone.render()` が読むのはこの 2 つだけで、他は全てコメントアウト済み
- `Disc` / `Triangle` の `thickness` → **削除**。どこからも読まれておらず、
  `PatchSolid` は厚みをサイズから算出している
- `Shinness` → **`shininess`**（Grasp3D 側の綴り誤り）
- キー末尾の空白除去、`LIGHT_0` の命名正規化

いずれも実施後に **8,240 サンプルの一致検証が緑のまま**であることを確認済み。

> ⚠ **安全網は残す。** `Cone` の死にプロパティは「値を書き込む経路」（デフォルトコンストラクタ）と
> 「書き込まない経路」（`Cone(radius, height)`）が両方あるため、`.gsf` によっては存在しうる。
> `Gsf2Json.DEAD_KEYS` が拾って件数を報告する。**黙って落とさない。**

### D-13: シェルはエンジンの CEF 層で表示する。ただしエンジンをリンクはしない

**決定**: `app/ui/shell.html` は MitiruEngine の `include/mitiru/cef/` をヘッダとして使って
表示する。Makina は `mitiru` ライブラリをリンクしない。エンジン側の CEF 層は
**D3D12 のハンドルだけを受け取る**入口を持つように直した（既存の `Dx12Device&` 版は残し、
そちらが新しいほうへ委譲する）。

**測ったこと**: CEF 層 5,199 行が `Dx12Device` に触っているのは **3 箇所だけ**だった。

| 呼び出し | 何のため | 生ハンドルなら |
|---|---|---|
| `device.nativeDevice()` | テクスチャとパイプラインの作成 | `ID3D12Device*` |
| `device.commandQueue()` | アップロードの投入 | `ID3D12CommandQueue*` |
| `device.getSwapChain()` | 合成先の RTV を取り出す | `D3D12_CPU_DESCRIPTOR_HANDLE` |

合成そのもの（`MitiruCefTexture::composite`）は**元から RTV ハンドルしか見ていない**。
つまりこの層は最初から D3D12 にしか依存しておらず、`Dx12Device` を要求していたのは
引数の型だけだった。

**なぜこの形か**:

- **(A) Makina が CEF を自前でホストする** — CefClient / OSR ハンドラ / 合成パスを書き直す。
  600〜900 行の定型で、既に解いてある問題をもう一度解くことになる
- **(B) Makina がエンジンの `Dx12Device` を採る** — 新しいコードは最小だが、
  D-08 と 3.1 が選んだ独立性を捨てる。`Dx12Device` はスワップチェーンもウィンドウも
  抱える 799 行で、モデラのレンダラをゲームエンジンのそれに縛る
- **(C) エンジン側の入口を D3D12 まで下げる** ← これ。新しいコードは両側合わせて 60 行ほど、
  既存の呼び出しは 1 つも壊れない

(C) を選んだのは安いからだけではない。**姉妹プロジェクトという主張が、これで具体になる** —
「エンジンの UI 層はエンジンに依存していないので、外から借りられる」は、借りて動かして
はじめて言えることである。借りられない層は、再利用可能と呼ぶ資格がない。

**残っている前提**: CEF の DLL 一式と `MitiruCefHelper.exe` は Makina 側にも配る必要がある
（`external/cef` は取得済み）。ここは共有ではなく複製であり、そう認めておく。
`web/mitiru_runtime/*.js` も同様に、シェルの隣へ複製する。

---

### D-08: DX12 で書く。Vulkan は最後に「証明」として引き上げる

- `gfx` 抽象に `Dispatch` が無く、上位レンダラ（`GBuffer`/`Deferred`/`SubsurfaceScatter`）が DX12 直書き。
  → Vulkan で先に書くと**統合先がゼロ**で並行世界になる。
- Windows 専用ターゲットなので Vulkan の移植性メリットが現時点でゼロ。
- 訴求は「両方書ける」ではなく「**同じパスが抽象越しに両方で同じ絵を出す**」。
  **これには DX12 版が参照として先に必要。**

### D-09: 距離場からメッシュを**抽出**しない。既にある境界表現は**書き出す**【改訂】

- 「UV もベイクも要らない」が最大の訴求。**距離場をグリッドでサンプルして再構成する**
  （Marching Cubes / Dual Contouring）と、角が丸まり、全体が解像度依存になり、
  結局 UV が要る。訴求を自分で否定するので、これはやらない。
- MitiruEngine 側は SDF ランタイムを持つ（Phase 6）。

**ただし境界表現は既にある。** `TessellateScene.hpp` の `tessellate()` が
`Bsp.hpp`（csg.js 移植）の多角形 CSG でソリッドを返す — 156,932 サンプルの
相互検証に使っている当のものである。これを書き出すのは**抽出ではなく直列化**で、
D-09 が禁じている性質を 1 つも持たない：

| | Marching Cubes | 既にある B-rep |
|---|---|---|
| 角 | 丸まる | 平面同士の交線なので**厳密** |
| 箱 | 数千三角形 | **6 面** |
| 解像度依存 | 全体 | **曲面だけ**（`kSegments = 24`） |

`MeshExport.hpp` が STL と OBJ を書く。**UV は付けない** — 付けたら D-09 の
言う自己矛盾そのものになる。CAD カーネルと同じ立て付けで、
「SDF で作れて、必要なら正確な B-rep として持ち出せる」は訴求を弱めない。

**第 4 の照合軸**（`mesh_compare`）: 書き出したメッシュが距離場の外に出ていないこと。
不変条件が**片側だけ**なのが要点で、「頂点が表面上にある」は**誤り**である —
`tessellate()` は重なりうるソリッドの列を返し、場はその和なので、
他のソリッドに埋もれた面は深く負になるのが正しい。

| シーン | 最悪の外れ | 許容 |
|---|---|---|
| cutaway_bug | 0.0188 | 0.0762 |
| hero_flange | 0.0149 | 0.0529 |
| pettobotoru | 0.0098 | 0.0519 |
| penrose / test / verify_transforms | 0.0000 | — |

**尺度を一度間違えた。** 最初は「その三角形自身の辺長」で測って 113 倍という値が出て、
不具合に見えた。実際は違う — 24 分割のトーラスを引くと、**削り残る量は
トーラス側の分割**で決まるのに、残るスライバは極小なので比が跳ねる。
`hero_flange` の最悪点は場の外 0.0087 で、`1.15 × (1 − cos(π/24)) = 0.0099` に一致した。
**誤差は「削られる面」ではなく「削る面」に属する。** 尺度はシーン半径に直した。

`verify_plane` だけは除外する。無限平面は半空間で**ソリッド形を持たない**ので、
有限の代用が尽きたところで削り残る（1.18 対 許容 0.066）。
ブーリアンの誤りではなく、半空間が有限であることそのものである。

---

### D-14: 変形は**領域ワープ**として、変換ノードの仲間に置く【2026-08-15 起案】

**動機**: 「Blender の Simple Deform（Twist / Bend / Taper）のような変形が欲しい」。
メッシュのモデラーは頂点を動かして変形するが、このモデルに頂点は無い。距離場で変形とは
**評価点の側を曲げる**ことである — `d'(p) = d(w⁻¹(p)) / L`。ワープ `w` の逆写像で点を
未変形空間へ送り、そこで既存の場を読む。既存のプリミティブ・ブーリアン・blob・sor 全部が
一行も変わらずに曲がる。**これはメッシュ側には無い訴求で、D2（追従して流れ直す）と同じ系統
の主張になる**: ワープを動かせば経年変化（曲率・上向き・窪み）はワープ後の場から読み直される。

**決定**:

1. **ワープは変換ノードの仲間**（`Op::Twist` / `Op::Bend` / `Op::Taper`、子を包む、
   `isTransform` と並ぶ `isWarp`）。Translate/Rotate/Scale が通っている全経路 —
   Edit / Selection / JSON / アウトライナ / ツールバー / プロパティ / makina_edit の CLI —
   が同じ形で扱える。ワープ専用の型や別ツリーは作らない。
2. **CPU 評価は 2 つのフックに乗る。** `invApply`（点の逆写像）と `scaleFactorOf`
   （距離補正）。ワープは距離を縮めうる（twist は半径 r で接線方向に k·r 伸び、
   bend も同様）ので、Lipschitz 定数 `L ≥ 1` で割って**下界を守る**。
   `L` は節点のパラメータとサブツリーの半径から決める — Bounds.hpp が半径を持っている。
   下界が壊れると max() が内部を消し、マーチが貫通する（memory: lower-bound-two-traps）。
3. **GPU は行列に畳めない。** Flatten は変換を葉の `inv` に焼き込むが、ワープは非アフィン。
   `EvalNode` に「ワープ列」の索引（`sorProfiles` と同じ side table 立て付け: 節点は
   `(offset, count)` を持ち、列は `{kind, axis, k, ...}` の 8 float）を持たせる。
   生成シェーダは葉ごとに `p = warpN(...warp1(p))` を直線コードで出す（D-04 の
   コード生成主義と整合、コンパイル時定数）。インタプリタは side table を歩く
   （sor / sweep と同じ「読めるがバッファに載らない」問題があるので、当面は
   `interpretable()` の対象外にし、ドラッグ中は生成版で描く）。
4. **正しさの門は 2 本足す。** (a) **格子勾配 ≤ 1 + ε**（数値勾配で Lipschitz を直接検査。
   ワープ下では SDF↔BSP の 156,932 点照合が使えないので、これが代わりの内部整合の軸）。
   (b) **POV 輪郭照合**。POV に twist/bend は無いが `isosurface { function { ... } }` で
   ワープした距離場を書ける（文法は公開リファレンス）。エクスポータはワープ下のサブツリーを
   isosurface 関数として書き、既存の IoU 門で比べる。POV の isosurface は `max_gradient` と
   `contained_by` が要り遅いので、フィクスチャは小さく保つ。
5. **B-rep 書き出し（D-09）は頂点をワープして書く。** BSP はアフィンしか扱えないので、
   ワープ下のソリッドは未変形空間でテッセレートし、頂点だけ `w` で動かす。面は曲がるべき
   ところで折れる（`kSegments` と同じ種類の解像度依存）。mesh_compare の片側不変条件
   （場の外に出ない）で許容内かを測り、出るなら細分割の必要を数字で知る。
6. **初期集合は Twist / Bend / Taper の 3 つ**（軸 + 係数、原点はノードの局所座標）。
   Displace（ノイズ）は POV で照合できない（VERIFICATION「照合しないと決めたもの」の
   ノイズ系と同じ理由）ので入れない。Lattice / FFD は「制御点の意味論」が sor / sweep の
   制御点編集と同じ未設計領域なので後。

**採らなかった案**:

- *ワープを葉ごとのパラメータにする*（各プリミティブに twist 係数）: ブーリアンの
  上に置けず、「この組み合わせを丸ごとひねる」が書けない。ノードにする理由そのもの。
- *EvalNode に 4x4 行列を 2 本持って前後で挟む*: 非アフィンなので意味が無い。
- *メッシュに落としてから曲げる*: D-09 の自己否定。

**完了条件**（2026-08-16 全部達成）:
- [x] Twist / Bend / Taper が `eval` で正しい形を出し、格子勾配 ≤ 1 + ε の門を通る
      （warp_check 28 checks、勾配 1.0000、木と程序の一致 6e-8）
- [x] 生成シェーダで描け、POV isosurface との輪郭 IoU が既存の門（0.99）を通る
      （twist 0.99723 / bend 0.99716 / taper 0.99095、warp-silhouette-check.bat）
- [x] JSON 往復（warp_check）。POV は isosurface で片方向、往復テストは名指しでスキップ
- [x] ツールバーに 3 ボタン、`add.twist` 等は Op テーブル駆動で受理、プロパティは
      `input:degreesPerUnit` / `input:ratePerUnit` が自動（viewport-check の warps ケース）
- [x] 経年変化パスがワープ後の場を読む: docs/images/weathered_twist.png —
      ねじれた稜線に沿って摩耗のハイライトが螺旋に走る。曲率を場から取るので何も足さずに追従した

**やってみて分かったこと**: 保守的下界の場は AO を暗くする（1/L に縮んだ場を「遮蔽」と読む）。
非一様 Scale でも同じ性質があったが補正が 1 に近く見えていなかった。生成ヘッダの
`MK_MIN_CORRECTION` で AO の単位を場に合わせた。ガードは立方体でなく**軸まわりの円柱**にしないと
L が角の分だけ（√3 倍）過大になる。

---

## 5. フェーズ

### Phase S — 技術検証スパイク【最優先・順序変更禁止】

**目的**: プロジェクト全体を破綻させうる 2 点を、着手前に実測で潰す。

**やること**:
1. DX12 の最小フルスクリーンパスで、**ハードコードした CSG（20ノード相当）をスフィアトレース**する。
   `Renderer3D_DX12` とは独立した使い捨てコードでよい。
2. 1280×720 / 最大 128 ステップ で **フレーム時間を実測**。
3. ノード数を 20 → 50 → 100 と増やして**スケーリングを測定**。
4. **AO のコーンマーチを追加**して、追加コストを測定（Phase 4 の主コストがこれ）。

**完了条件**:
- [ ] 20 ノード / 720p / AO なし で **16 ms 以下**
- [ ] 50 ノード / 720p / AO あり で **33 ms 以下**（30fps。編集中はこれで十分）
- [ ] 測定結果を `docs/SPIKE_PERF.md` に記録

**未達だった場合の分岐**（先に決めておく）:
- 解像度を下げる（720p → 540p）＋ 上方向スケーリング
- ステップ数を下げ、**プログレッシブ蓄積**（静止時に精度を上げる。TAA が既にあるので流用可）
- ノード上限を 256 → 64 に引き下げ
- **上記でも未達なら D-04 を覆してコード生成方式へ**（この判断はここでしか行わない）

**想定工数**: 2〜4 日

---

### Phase 0 — 型設計とシーン記述

**目的**: 1 つの型が「`GameMemory`」「GPU バッファの元」「交換フォーマット」の 3 役を兼ねる。
**ここを間違えると全フェーズが歪む。**

**やること**:
1. `CsgNode` の確定（D-02 をベースに、Grasp3D の全プリミティブを表現できるか検証）
   - ⚠ `SuperEllipsoidBox`（842行）は SDF が非自明。**閉形式が無ければ Phase 0 でスコープ外に落とす。**
     Grasp3D の `SceneSdf` も「SuperEllipsoidBox は skip」と明記している。
2. ~~座標系の統一~~ ✅ **完了** → [COORDINATES.md](docs/COORDINATES.md)
   - 右手系・Y-up・度・メートル。**Grasp3D と MitiruEngine 主経路が既に一致、変換は恒等**
   - 制約は 2 点：**投影の深度レンジは [0,1]**（`GlmBridge`、`sgc::Mat4f::perspective` は不可）／
     **POV 出力は `right` を負で書く**（忘れると鏡像）
3. ~~単位系の確定~~ ✅ **1.0 = 1 m に確定**（COORDINATES.md §2）
4. **ノード ID の安定性**（PR 差分・AI 操作・アニメーション参照が全て安定 ID を要求する）
   - 添字はノード追加削除で動く → **添字とは別に不変 ID を持たせるか**を決める
5. JSON シリアライズ形式の確定（`.gsf` は Java ネイティブシリアライズ＝バイナリなので使わない）
6. `MITIRU_REFLECT` の宣言
7. ~~平坦化はサブツリーのグルーピングを保つこと~~ ✅ **完了**（[CSG_NODE.md](docs/CSG_NODE.md) §7）
   - **Grasp3D の `Difference` / `Intersection` は既に n 項**だった
     （`d = max(d0, -min(blades))`）。「8 個の穴をまとめて差分」は既に 1 ノード 9 子で表現済み。
     **グルーピングは発明せず、壊さないだけでよい**
   - Phase S で「左寄せ退化木」と書いたのは**スパイクのシーン生成器の都合**であり、
     Grasp3D の構造ではなかった
   - 二分化は平坦化時のみ、かつ**平衡に**行う。スタック深さが n → ⌈log₂n⌉+1 になり、
     サブツリー AABB が初めて意味を持つ
   - ⚠ **オーサリングツリーは自動平衡化しない。** ユーザーの並べた順序は設計意図であり、
     組み替えると「設計意図がモデルに残る」という主張が崩れる

**完了条件**:
- [ ] `CsgNode` が Grasp3D のプリミティブ・ブーリアン・トランスフォームを全て表現できる（表で確認）
- [ ] 座標系変換規約が文書化され、`Coord.hpp` に実装されている
- [ ] `GameMemory` サイズが実測され、巻き戻し 10 秒で 32 MB 以下
- [ ] JSON の読み書きが往復して一致する（ラウンドトリップテスト）
- [ ] Grasp3D の `test.gsf` / `penrose.gsf` / `pettobotoru.gsf` を JSON に変換できる

**想定工数**: 1〜2 週

---

### Phase 1 — makina-core（ヘッドレス）

**目的**: GPU なしで、CSG とその評価が正しいことを確定させる。**テストは全部ここに書く。**

**やること**:
1. `Sdf.hpp` — プリミティブの SDF を **C++/HLSL 共用**で記述（D-03）
2. `Eval.hpp` — `SceneSdf.java` の移植（CPU 評価器）
3. `Flatten.hpp` — オーサリングツリー → RPN 評価プログラム（トランスフォーム焼き込み、AABB 付与）
4. `Bounds.hpp` — `SceneBounds` 移植
5. `Bsp.hpp` — `CsgBsp` 移植（**検証用の第 2 実装**。描画には使わない）
6. `Measure.hpp` — `SceneMeasure` 移植
6b. **`PatchSolid.hpp` — 厚みゼロ面の CSG 対応ルール**（[CSG_NODE.md](docs/CSG_NODE.md) §4.3）。
   平坦化時に厚みを焼き込む。オーサリングツリーは厚み 0 のまま保つ（ユーザーが見ている値と一致させる）
7. `PovExport.hpp` — POV-Ray 出力（**検証用の第 3 実装**）
8. `Serialize.hpp` — JSON 入出力

**完了条件**:
- [ ] ユニットテストのカバレッジ 80% 以上
- [ ] **Java 版と C++ 版で、同一シーンの SDF 評価が一致**（グリッドサンプリング比較、許容誤差込み）
- [ ] Grasp3D の既存 `.gsf` 3 点を読み込み、POV-Ray 出力が Java 版と同等の絵を出す
- [ ] `makina-core` が MitiruEngine にも DX12 にも依存していない（CMake で確認）

**想定工数**: 3〜5 週（移植の本体）

---

### Phase 2 — SDF レイマーチパス（DX12）

**目的**: 画面に CSG が出る。**初めて絵が出るフェーズ。**

**やること**:
0. **【最優先】「構造のみ特殊化」版の評価と、ハイブリッド要否の判定**
   - Phase S でコード生成が 4.75 倍と確定済み（[SPIKE_PERF.md](docs/SPIKE_PERF.md) §6）。
     残る問いは**ドラッグ中の再コンパイル（92〜232 ms）をどう回避するか**
   - **測ること**: 木の構造だけを特殊化し、寸法と変換はバッファから読む版。
     定数畳み込みは失うが、スタック除去・分岐除去・直線化は保たれる
   - **十分速ければハイブリッドが不要になり、実装が大幅に簡単になる**
     （パラメータのドラッグは木の構造を変えないため、再コンパイルが不要になる）
   - 不十分なら、ドラッグ中インタプリタ / 静止時コード生成のハイブリッドを実装する
   - ⚠ **ノード単位 AABB カリングは Phase S で試して効果ゼロだった。繰り返さないこと**
     （テスト費用 ≒ プリミティブ評価費用のため。[SPIKE_PERF.md](docs/SPIKE_PERF.md) §4）
   - サブツリー単位のスキップは、**木の平衡化とセットでなければ意味がない**。単独で着手しない
1. `MitiruEngine/include/mitiru/render/csg/CsgRaymarchPass.hpp`
   - ⚠ `render/sdf/` はフォント用。**`render/csg/` を使う。**
2. RPN 評価プログラムを StructuredBuffer にアップロード
3. HLSL のインタプリタシェーダ（固定長スタック、深さ 16）
   - ⚠ 動的添字ローカル配列がスクラッチに落ちる懸念は、Phase S のシーン（スタック深さ 2）では
     顕在化せず。**深い木での再検証が必要**
4. `Sdf.hpp` を C++ と HLSL の両方から include できるビルド設定
5. DXC のビルド統合（`-T cs_6_0`、将来の `-spirv` を見据えた設定）
6. 既存 `GBuffer` への書き込み（深度・法線・マテリアル ID）
   → **これで既存の Deferred / Shadow / TAA / PostProcess がそのまま乗る**

**完了条件**:
- [ ] Grasp3D の `.gsf` から変換したモデルが画面に出る
- [ ] Phase S の性能目標を満たしている
- [ ] 既存の影・ポストプロセスが SDF オブジェクトに適用される
- [ ] **CPU 評価器と GPU の距離値が一致**（同一点をサンプルして比較するデバッグモード）

**想定工数**: 3〜4 週

---

### Phase 3 — Makina アプリ（CEF / HTML）と操作性

**目的**: 編集できる。3 入力口が同じツリーを触る。
**そして、アーティストが違和感なく触れる操作性を持つ**（D-10）。

**やること**:
1. [x] CEF UI シェル（ツリービュー、プロパティ、ビューポート）— Grasp3D の配置のまま。
   ページはエンジンの CEF 層で出すが、エンジンはリンクしない（D-13）
2. ツリー編集操作（ノード追加・削除・付け替え・パラメータ変更）
3. [x] bridge（signal-only）: JS → C++ 操作イベント、C++ → JS 状態 push。
   ページから木へ届く道は 3 本あり、**どれも他の検査では踏まれない**ので専用の入口を作った:

   | 道 | 押すもの | 検査 | 対照 |
   |---|---|---|---|
   | ボタン | ツールバーのミュート | `button` | ボタンの外を押す |
   | 行 | アウトライナのノード | `outliner` | 行の外を押す |
   | 欄 | プロパティの数値 + Enter | `field` | 数字でない文字を打つ |

   C++ → ページは `ViewState.hpp` が語彙を決め、`shell_audit.py` が HTML と突き合わせる
   （BINDING.md が約束して実装されていない静的照合が、これである）
4. **Undo/Redo は実装しない。** エンジンの巻き戻し（ring buffer）に委譲する
5. CLI からの編集経路
6. **操作性の実装（下記チェックリスト）**

#### 3.A ビューポート操作【最優先・最も費用対効果が高い】

| 機能 | Maya（既定） | Blender（プリセット） | 備考 |
|---|---|---|---|
| オービット | `Alt` + 左ドラッグ | 中ドラッグ | **選択物を軸に回る**（ワールド原点ではない） |
| パン | `Alt` + 中ドラッグ | `Shift` + 中ドラッグ | **移動量が視距離に比例する** |
| ズーム / ドリー | `Alt` + 右ドラッグ / ホイール | ホイール | カーソル位置に向かってズーム |
| 選択物にフィット | `F` | `テンキー .` | **これが無いと全ての作業が遅くなる** |
| 全体にフィット | `A` | `Home` | |
| 正面 / 右 / 上 | — | `テンキー 1 / 3 / 7` | 反対側は `Ctrl` 併用 |
| 平行投影切替 | — | `テンキー 5` | Grasp3D の `LOOK_FROM_X/Y/Z` を置き換える |

> ⚠ Grasp3D の `dca118a` はまさに「オービット軸」と「距離比例パン」の修正。
> **この 2 つは体感品質を決める。最初に実装し、最後まで壊さない。**

#### 3.B ギズモと数値入力

- [ ] `W` / `E` / `R`（Maya）= 移動 / 回転 / スケール ※Blender プリセットでは `G` / `R` / `S`
- [ ] **軸拘束**: ギズモの軸ハンドルをドラッグ、またはドラッグ中に `X` / `Y` / `Z` を押す
- [ ] **ドラッグ中の数値直接入力**（Blender 流）: `G` → `X` → `5` → `Enter` で「X に 5 動かす」
  - **CSG は寸法駆動のモデリングなので、これが最も効く**
- [ ] **スナップ**: `Ctrl` 押下でグリッド / 増分スナップ
- [ ] ヘッダに現在の操作と数値をライブ表示（Blender 流）
- [ ] 数値フィールドの**横ドラッグでスクラブ**（Blender 流）
  - ⚠ Grasp3D の `ab9e3b7`（数値欄の ↑↓ がフォーカス移動してしまう問題）はここで構造的に解決する

#### 3.C 選択とアウトライナ

- [x] クリックで選択、`Shift` + クリックで追加（トグル）、空白クリックで解除
- [x] **多選択**。変形・削除・複製・フィット・ハイライトのすべてが選択集合に対して動く。**規則は 1 つ** — 編集は「他の選択に含まれていない選択」にだけ届く（`Selection.hpp` の `topLevel`）。これが無いと、親と子を選んで動かすと子が二重に動き、削除は 2 件目が拒否されてジェスチャ全体が捨てられる
- [x] ボックス選択（ドラッグ）。**クリックと同じ梯子で解決する**ので、矩形が選ぶものとクリックが選ぶものが食い違わない。判定はノードの世界 AABB を投影した矩形どうしの重なり — 面そのものではないので斜めに寝た細長い立体は実際より広く取る。見えている物を取り逃すより、隣を巻き込むほうが手の滑りとして読めるため、寛容な側に倒してある
- [x] **アウトライナ ↔ ビューポートの選択同期**（双方向）。ハイライトは C++ → ページ、行のクリックはページ → C++。行が運ぶ id で直接選ぶ — ビューポートの `select.pick` は「カーソルの下」なので、そのまま流すとパネルの上からシーンへレイを飛ばすことになる
- [ ] アウトライナ上のドラッグ&ドロップで親子付け替え（Grasp3D の `DnDTree` の思想を継承）
- [ ] 複製（`Ctrl+D`）
- [x] **ミュート**（`H`）。「非表示」ではない — CSG では**形が絵**なので、削り側をミュートすれば穴は埋まる。それは表示の切り替えではなく編集である。そう名乗らせたうえで、`Edit.hpp` の `withoutMuted` **一箇所だけ**が旗を解釈し、評価・境界・BSP・テッセレーション・POV 出力には**ミュート済みの部分木が抜けた普通の木**が渡る（8 つのヘッダが各自で旗を見る設計は、いずれ 1 つが忘れて絵とファイルが食い違う）。何も選ばずに `H` で全解除 — アウトライナが無い今、描かれていないノードは選べないので、戻れないミュートは名前を偽った削除になる
- [x] 分離表示（isolate）。Blender の `/`。選択の部分木と**先祖の連鎖**だけを描く view-time フィルタで、`withoutMuted` と同じ場所（`visible()`）に積む。トグルで固定し、往復すると絵は画素単位で元に戻る（編集ではないことの検査そのもの）

#### 3.D 参考にするが真似しないもの【スコープ防衛】

以下は**明示的に実装しない**。ここを守らないと Phase 3 が無限に伸びる（R-12）。

- Blender のモーダルキーマップ全体系（`Tab` でのモード切替、エディットモードの全機能）
- Maya のマーキングメニュー（放射状メニュー）
- マルチビューポートレイアウト（4分割等）
- ワークスペースのカスタマイズ / ドッキング自由化
- Blender の N パネル / T パネルの完全再現
- カスタムシェルフ・ツールバーの編集機能

**完了条件**:
- [ ] GUI でブーリアンを組み、パラメータをドラッグすると即座に画面が追従する
- [ ] Undo が巻き戻しで動く（追加実装なしで）
- [x] CLI から同じ編集ができる。**ビューポートに操作を足したらコマンドにも足す** — ミュートは足し忘れて片方だけにあり、さらに**移動・回転・拡縮はそもそもコマンドが無かった**（`move` は親子の付け替えである）。つまりスクリプトはノードを木の別の場所へ移せても1 ミリも動かせなかった。2026-08-14 に `translate` / `rotate` / `scale` / `mute` を追加し、`command_check` で塞いだ
- [ ] JS 側に状態を持っていない（ツリーの正は `GameMemory` のみ）
- [ ] **3.A の全項目が動作する**
- [ ] **3.B の軸拘束と数値直接入力が動作する**
- [ ] キーマップが設定ファイルに外出しされ、Maya / Blender プリセットが切り替わる
- [ ] **Maya か Blender の経験者に触ってもらい、「迷わず動かせた」ことを確認**
  （※ 共有チャットで指摘のあった「独りよがりでなく作れる人」の証明。可能ならバイト先で）

**想定工数**: 6〜9 週（操作性の作り込みで Phase 3 が最大の山になる）

---

### Phase 4 — ジオメトリフィールドと経年変化【D2：ヒーローショット】

**目的**: **このプロジェクトの主張が絵になる。**

**やること**:
1. SDF からのフィールド導出（HLSL）

| フィールド | 導出 | 用途 |
|---|---|---|
| AO / キャビティ | 距離場のコーンマーチ | 窪みの汚れ |
| カーバチャ | 勾配の発散（二階微分） | エッジ摩耗 |
| シックネス | 内向きレイマーチ | SSS（既存 `SubsurfaceScatter` に接続） |
| 上向き度 | 勾配 = 法線、そのまま | 埃 |
| エッジまでの距離 | 距離場そのもの | 縁の演出 |

2. フィールドを合成するマテリアルグラフ（HTML/CSS/JS のノードエディタ）
3. ヒーローアセット（摩耗した機械部品）の制作
4. デモ録画

**完了条件**:
- [ ] ブーリアンをドラッグすると、摩耗・汚れ・埃が追従して流れ直す
- [ ] 30 秒のデモ録画が撮れている
- [ ] 編集中のフレーム時間が 33 ms 以下

**想定工数**: 4〜6 週

---

### Phase 5 — 3 系統クロスチェック【D5：安い】

**目的**: 「派手なものを作ったうえで、正しさも担保している」。**主役ではなく脇役として置く。**

Phase 1 で BSP と POV 出力は既に実装済みなので、**比較ハーネスを書くだけ。**

**やること**:
1. 同一シーンを 3 系統で描画（BSP メッシュ / SDF レイマーチ / POV-Ray）
2. **形状の比較**（色ではない）: シルエット IoU / 深度差 / 法線角度差
3. 複数カメラ角度でのバッチ実行
4. CI に載せる（GitHub Actions。※現状 Grasp3D 側に CI は無い）

**完了条件**:
- [x] 3 系統の形状一致が数値で出る
      — SDF↔B-rep は内外判定 156,751 サンプル一致（`bsp_compare`）、
      SDF↔POV-Ray はシルエット IoU 0.9949〜0.9996（`silhouette-check`）
- [x] 閾値超えでレポートが出る
      — 許容量は**周長の倍数**（既定 2.0）。差分画像 `*_iou.bmp` も書く
- [x] CI で自動実行される — ただし**限定つき**。
      linux-gcc / windows-msvc / macos-clang で「Java も GPU も要らない検査」だけを走らせる。
      Grasp3D との一致と POV-Ray との突き合わせは runner に道具が無いので `verify-all.bat` のまま。
      **CI が緑でも移植の一致検証を通ったことにはならない**ので、ログにそう書き出している

**実測でのずれと、その理由**:
- 差分は必ず**マーチ側だけ**に出る（POV 側だけの画素は全シーンで 0）。
  `d < hitEps` で止まるので真の面より手前で当たる。系統的なので周長基準の許容量で吸収する
- `verify_plane` は比較対象から外す。POV に far 平面が無く、無限平面のシルエットは
  far 距離で決まってしまう。差分画像の境界がまさに far の円だった

**やり方の要点**: 比べるのは**色ではなくシルエット**。
シルエットはジオメトリ・変換・カメラ・利き手だけに依存し、
色はそれに加えてシェーディングとトーンマップにも依存する。
**1 つの数字で 4 つを同時に検査できる**のがシルエットを選んだ理由である。

**想定工数**: 1〜2 週 → **実績 1 日弱**（Phase 1 で BSP と POV 出力を済ませてあったため）

---

### Phase 6 — MitiruEngine 上での動作【D3】

**目的**: 姉妹プロジェクトである実体を示す。

**やること**:
1. エンジン側に SDF ランタイム（`makina-core` をリンクし、同じ評価器を使う）
2. ゲームシーン内に SDF オブジェクトを配置するサンプル
3. 既存の物理・ライティングとの統合（SDF は衝突判定にそのまま使える）

**完了条件**:
- [x] Makina で作ったモデルが、エンジンのサンプルゲーム内で動く — `examples/csg_solid` 章。
      `s.drawSolid("...csgbake.json", pos, rotY, scale)` の 1 行で、hero_flange が
      エンジンのメッシュと同じフレームに出る。深度も画素で確認した — 床のスラブに
      沈めると床下の部分だけが消える（CSG はメッシュの後に描かれるので、描画順では
      説明できない）。配管は Screen::drawSolid → IRenderer3D → renderCsgPass
      (clod の直後・OIT の前、MSAA HDR + depth)
- [x] **モデラーとエンジンで絵が一致** — golden ではなく **CPU の距離場との一致**で確かめた
      （13,311 画素すべて一致）。golden は「前と同じか」しか言えず、
      最初から間違っていれば間違ったまま固定される

**進捗**: 1 の CPU 側が通った（`d6421891`）。
`MITIRU_HAS_MAKINA` で任意依存、`mitiru/render/csg/CsgSolid.hpp` が
distance / contains / normal / bounds / raycast をエンジンの型で返す。
テスト 5 件（`[csg]`）。

> **GPU パスより先に CPU 側をやった**のは、依存方向（makina-core ← MitiruEngine）が
> 本当に一方向で通ることを示すため。先に GPU パスを書くと、
> それがウィンドウとデバイスの後ろに隠れてしまう。
> ついでに、これは**メッシュにはできない部分**でもある — 衝突・接地・スイープが
> 描画と同じ定義から出るので、コライダとモデルがずれない。

**GPU パスも通った。** `mitiru/render/csg/CsgRenderPass.hpp` が、焼いた DXIL から PSO を作り、
三角形 1 枚で距離場をレイマーチして **色と深度**を書く。

モデラー用のラッパ（`scene_shading.hlsl`）はフレーム全体を持つ前提なので、そのままでは載らない。
`scene_engine.hlsl` を足して、違いを **3 点だけ**にした（`evalCsg` は同じ生成関数なので、
絵が食い違えばラッパしか原因が無い）:

| | なぜ |
|---|---|
| オブジェクト空間で歩く | 小道具は原点に無い。焼いた `evalCsg` に置き場所を教えない |
| 立体の箱でクリップ | 画面の 1/10 の小道具に全画素分のステップ予算を払わない |
| `SV_Depth` を書く | 手前のメッシュが手前のままでいられる |

置き方は**剛体変換 + 一様スケールのみ**。非一様スケールを掛けた距離場はもう距離ではなく、
潰した軸で値が短く出てマーチが表面を突き抜ける。`draw` は描かずに拒否する。

**検証は golden 画像ではなく CPU の距離場との一致で行う。**
golden は「前と同じ絵か」しか言えず、最初から間違っていれば間違ったまま固定される。
画素ごとにシェーダと同じ式でレイを組み、`CsgSolid::raycast` に当たるかを訊いて突き合わせる
（13,416 画素、一致率 99% 以上を要求。輪郭は MSAA と FXAA が混ぜるので近傍が割れる画素は除外）。

> **それで実際に 1 件見つかった。** `Camera3D::fov()` は**ラジアン**（既定 1.0472 = 60 度）
> なのに度として変換していた。扇が 115 倍狭くなるので小道具が画面を埋め尽くす — が、
> **「何か描けている」ので絵を眺めるだけでは間違いに見えない**。
> 気付けたのは、シザーを詰めるのに使っている `viewProjectionMatrixZO` が**第 3 の情報源**として
> レイの扇と食い違ったからである。CPU と GPU が同じ式を共有している以上、
> 両方が同じように間違えば一致してしまう。独立した 3 つ目が要る。

**深度合成も検査済み。** 同じ板を小道具の手前に置けば隠れ、奥に置けば隠れない。
後者が無いと「メッシュが後から描かれるので常に勝つ」実装でも通ってしまう。
深度テストを切って落ちることも確認済み（小道具が手前の板を塗り潰す）。

エンジン側の全テストは 2,732 件 / 失敗 0。

**残り**: サンプルゲームへの配置。

> **GPU パスの方式は測って決めた。** インタプリタ（`StructuredBuffer` に評価プログラムを積む、
> 実行時コンパイル不要）を実シーンで測ると、codegen に対して
> **25 ノードで 5.6 倍、75 ノードで 11.4 倍**遅い（min-of-25 × 3 回、SPIKE_PERF.md §9）。
> ノードが増えるほど開く。1280×720 でプロップ 1 個に 55 ms は載らない。
> **アセットインポート時にオフラインで DXIL を焼く**のが答えで、
> 実行時コンパイラも要らず速度も落ちない。
> インタプリタは残す — 生成コードと**画素完全一致**するので、照合相手として使える。

**想定工数**: 2〜3 週

---

### Phase 7 — AI からの編集【D4】

**目的**: 「AI が使っていて強い」の実証。**エンジン側で 8 割実装済み。**

**やること**:
1. `MITIRU_REFLECT` によるツリーの JSON 化（Phase 0 で宣言済み）
2. bridge API に編集コマンドを追加（Grasp3D の `BridgeServer` / `BridgeCommands` を参考）
3. AI エージェント（Claude Code 等）からの操作デモ
4. 反実仮想（試す → 見る → 戻す）を巻き戻し機構で実現

**完了条件**:
- [ ] 自然言語の指示でツリーが変更され、画面に反映される
      — コマンド列までは通る（`makina_edit apply`）。自然言語からの変換は上位の仕事で、
      画面への反映は Phase 3 待ち
- [x] AI がツリー全体を構造化 JSON で読める — `makina_edit describe`
- [x] 「試して戻す」が動く — `History.hpp`。`Scene` が flat POD なのでスナップショットはコピー、
      巻き戻しは厳密（`edit_check` がバイト単位で検査）
- [x] **（追加）AI が自分の仕事を数値で確かめられる** — `makina_edit measure`。
      これが無いと、エージェントは絵から推測するしかない

**実装済み**: `Edit.hpp`（id 指定・木の作り直し）・`History.hpp`・`Command.hpp`（JSON コマンド）・
`tools/makina_edit.cpp`（describe / apply / measure）。詳細は docs/AGENT_API.md。

**想定工数**: 1〜2 週 → **コア側は 1 日弱**。残りはブリッジ結線（Phase 3 依存）

---

### Phase R — レンダラの充実（POV の機能を、照合できる形だけ）

**計画外の追加。** 発端は「POV はオープンソースなんだから機能をたくさん入れたい」で、
調べたところ**これは検証が強くなる方向の変更**だった —
`finish` と `pigment` を **POV 互換で**実装し、エクスポートと描画で同じ数値を使えば、
比較の粒度が**輪郭から画素**に上がる。詳細は `docs/VERIFICATION.md`。

> **ライセンス**: POV-Ray 3.7 以降は AGPLv3。ソースを持ち込むと Makina 全体が AGPL になるので、
> 取り込むのは**仕様と挙動**だけで実装は自前、という線を引いてある。
>
> **この線は `licence-check.bat` が毎回引き直す**（`verify-all` から）。
> 他の主張は破れても絵が間違うだけだが、これは**プロジェクトのライセンスが変わる**。
> しかも破れ方は同じく静かで、式に詰まった誰かが POV のソースを開き、
> 書き直すより楽だからと補助関数を 1 つ残し、ビルドは何も言わない。
> AGPL の文面・POV-Ray の著作権表示・`Persistence of Vision` の名前を木の中から探し、
> 見つかれば落ちる。
>
> **読んで記憶で書き直したものは、どんな検査でも捕まらない。** そこを守るのは方法のほうである —
> `pov_filter_probe.py` と `pov_camera_probe.py` が存在するのは、
> **動いているプログラムから数字を測った**のであってソースから読んだのではない、という履歴そのものである。

| | 内容 | 状態 |
|---|---|---|
| R-1 | `finish` 互換の陰影。マテリアルが絵に届く | ✅ 平均差 0.14〜4.76 |
| R-2 | `pigment` — checker / gradient / radial + `color_map` | ✅ 立体テクスチャ、切り口をまたいで連続 |
| R-3 | シーンが光を持つ。複数光源・減衰・**距離場のソフトシャドウ** | ✅ 硬い影で POV と平均 3.49 |
| R-4 | カメラ種別 — orthographic / fisheye / ultra wide angle / **panoramic** | ✅ 3.49 / 3.90 / 3.58 / 4.20 / 3.81（正方形フレーム）|
| R-5 | `reflection`（1 バウンス） | ✅ 平均 3.78。切ると 1.71% の画素が動く |
| R-5a | `filter` の透過（面を越えて積む、POV と同じ 5 層） | ✅ glass 0.27 / ペットボトル 4.02 |
| R-5b | 屈折（`interior{ior}`、全反射込み） | ✅ 平均 0.50 |
| R-6 | パターンが物体と一緒に動く（下記 R-15） | ✅ pigment 2.83 / refraction 0.50、戻すと 14.9 / 114 |
| — | メッシュ書き出し（B-rep をそのまま。D-09 改訂） | ✅ STL / OBJ、第 4 の照合軸 |
| — | **POV インポータ**（部分集合） | ✅ 往復が全 12 フィクスチャで**幾何・外観とも 0.00e+00** |
| #33 | **ネットの .pov を 1:1 で**（blob / sor / sphere_sweep・#macro / #include / rand・brilliance / diffuse / 多 stop map・4096 ノード） | ✅ pingu.pov 95 ノード IoU 0.996 / mean 2.62、scene.pov 2285 ノード往復 0.00e+00。残件は全部「照合しないと決めたもの」の表（VERIFICATION.md） |
| — | 往復に**外観の軸**を足す（評価器が選ぶものを比べる） | ✅ 入れた時点で 3 件の欠落が出た |
| — | 往復に**光源**を足す（ の読み込み） | ✅ 差 0.00e+00。指向性と softness は理由つきで対象外 |

**影はこのモデルが POV より強い場所である。** レイトレーサは面光源と多サンプルを要する半影を、
スフィアトレースは**すでに回しているループから 1 回**で出す。
硬い影だけは POV と一致を主張できる形に保ってあり、それより柔らかいものは照合対象外と明記してある。

**カメラも同様に安い。** ラスタライザなら射影行列と下流全部を合わせる必要があるところが、
fisheye は 4 行で済む。**だからこそ検証した** — 4 行でも 4 行ぶん間違えられるので。

---

### Phase 8 — Vulkan への引き上げ【証明】

**目的**: 抽象設計が正しかったことの証明。**絵が変わらないことが成果。**

**やること**:
1. `ICommandList` に `Dispatch` を追加、`IPipeline` にコンピュートパイプラインを追加
2. DX12 / Vulkan 両バックエンドに実装
3. `CsgRaymarchPass` を抽象越しに書き直す
4. HLSL を DXC の `-spirv` で SPIR-V 化
5. **両バックエンドで同じ絵が出ることを Phase 5 のハーネスで確認**

**完了条件**:
- [ ] 同一シーンが DX12 と Vulkan で同じ絵を出す（数値比較）
- [ ] シェーダソースを書き分けていない

**想定工数**: 3〜4 週

---

## 6. リスク登録簿

| ID | リスク | 影響 | 対策 | いつ |
|---|---|---|---|---|
| **R-01** | ~~座標系の不一致~~ → ✅ **解消**（[COORDINATES.md](docs/COORDINATES.md)）。Grasp3D も MitiruEngine 主経路も**右手系・Y-up で一致**しており、**変換は恒等**。`Coord.hpp` は作らない | ~~致命的~~ **解消** | 残る制約は 1 点のみ：**投影は必ず深度レンジ [0,1]**（`GlmBridge::perspective`）。`sgc::Mat4f::perspective` は GL 規約 [-1,1] なので使わない | 完了 |
| **R-02** | ~~iGPU の性能不足~~ → ✅ **完全解消**（[WEATHERING.md](docs/WEATHERING.md) §1）。Phase 4 のフィールド 4 種を毎ピクセル導出しても **+18〜41%** で済んだ（見積もりは 2〜3 倍だった）。`pettobotoru` が経年変化込み **9.86 ms = 101 fps**。**2026-08-14 に測り直して 8.9 ms** — 透過・屈折を入れたあとも回帰なし（±5% の測定ノイズ内）。`spike/perf-check.bat` が `verify-all` から毎回測る | ~~致命的~~ **解消** | **540p + アップスケールの最終防衛線は不要になった。** ※ノード単位 AABB カリングは効果ゼロ、繰り返さないこと | 完了 |
| **R-03** | **CSG 差分の SDF が非厳密**（`max(-a,b)` は保守的下界）→ スフィアトレースが表面を突き抜ける | 中。継ぎ目にアーティファクト | ステップスケール < 1 を導入。`SceneSdf.java` が既に「保守的下界」と明記しており、Java 版で挙動を確認できる | Phase 2 |
| **R-04** | **カーバチャのノイズ**（距離場の二階微分はサンプル幅に敏感） | 中。エッジ摩耗が汚くなる | 複数スケールでサンプルして平滑化。Phase 4 の実装上の山場として工数を積んでおく | Phase 4 |
| **R-05** | **`GameMemory` サイズ肥大**で巻き戻しリングバッファが破綻 | 中 | D-02 でトランスフォームを inline しない設計にした。Phase 0 で実測（32 MB 上限） | Phase 0 |
| **R-06** | ~~`SuperEllipsoidBox` の SDF~~ → ✅ **クローズ。落とす**（[CSG_NODE.md](docs/CSG_NODE.md) §4.4）。NURBS 制御点ベースで閉形式 SDF が書けず、**Java 版 `SceneSdf.supported()` も対象外**にしている＝既存機能の後退にならない | 解消 | `.gsf` に含まれていたら読み飛ばし、**警告して件数を報告**する（`countUnsupported` と同じ思想）。黙って消さない | 完了 |
| **R-13** | **非一様 Scale で歩幅が縮む**（[CSG_NODE.md](docs/CSG_NODE.md) §4.1）。X/Y/Z 独立スケールでは距離を正確に戻せず、Java 版と同じ「最小軸倍率」で保守的下界にするしかない。安全side だがステップ数が増える | 中 | 式は Java 版に合わせる（変えると Phase 1 の一致検証が成立しない）。**Phase 2 の性能測定を一様スケールのシーンだけで行わない**こと | Phase 2 |
| **R-07** | ~~ノード添字の不安定性~~ → ✅ **クローズ**（[CSG_NODE.md](docs/CSG_NODE.md) §6）。`uint32 id` を単調増加で払い出し、**再利用しない**。`params[12]` にした時点のパディングに収まるので**サイズコストはゼロ** | 解消 | 既知の穴：巻き戻すと `nextId` も戻るため、外部に持ち出された ID は別ノードを指しうる。**実際に問題になるまで対策しない** | 完了 |
| **R-08** | **`gfx` 抽象にコンピュートが無い** | 低（既知） | D-08 で DX12 直書きを選択済み。Phase 8 まで触らない | — |
| **R-15** | ~~ピグメントが世界座標で引かれている~~ → ✅ **解消**。パターンの空間は**エクスポータが書く .pov が決める**：`Pov.hpp` は変換を葉のプリミティブまで押し下げるので、ブーリアンに付いた `pigment` の後には変換が無く POV は世界座標で読み、プリミティブに付いたものは直後の `translate` で一緒に動く。平坦化がこの分岐をそのまま持つ | 解消 | ピグメント表は `EvalProgram` が持ち、(パターン, 置かれた空間) の組ごとに 1 エントリ。`EvalNode` の余りワードがその添字を運ぶ。**回転も拡縮も行列なので扱える** — `Pigment` の scale/translate に畳み込む案では回転が落ちていた | 完了 |
| **R-09** | **スコープクリープ**（全部作ろうとして何も完成しない） | **高** | §2 の完成の定義と MVP ライン。**Phase 4 到達を最優先**し、それ以外は切れる設計にしてある | 常時 |
| **R-12** | **操作性の作り込みが無限に伸びる**（Blender / Maya は数十年の蓄積であり、追いかけると終わらない） | **高** | §5 Phase 3.D に「真似しないもの」を明示。**3.A → 3.B → 3.C の順で実装し、3.A が終わるまで 3.B に入らない**。3.A だけでも体感品質は激変する | Phase 3 |
| **R-10** | **Java 参照実装の喪失**（Grasp3D を壊すと検証基準が消える） | 中 | **凍結範囲を限定する**：`SceneSdf` / `CsgBsp` / `CsgTess` / `SceneBounds` / `SceneMatrix` / `SceneMeasure` / プリミティブの幾何的意味 / POV 出力は**触らない**。UI・操作性・ツールの改善は**可**（検証基準に影響しないため） | 常時 |
| **R-14** | ~~既存コーパスが `Plane` / `Disc` / `Triangle` に触れていない~~ → ✅ **解消**。`tools/makescenes` が検証シーン 3 本を**プログラムで生成**し、`Plane` / `Disc`（穴あり・なし）/ `Triangle` / 3 軸の回転 / 非一様 Scale / 負の Scale / 負の高さ Cone / 入れ子変換を網羅。**7 シーン 14,420 サンプルで Java と一致** | 解消 | `PatchSolid` は D-12 により移植しないので、厚みゼロ面の CSG は検証対象から外れた | 完了 |
| **R-11** | **就活スケジュールとの衝突** | **高** | §9 の未決事項。締切が判明した時点でフェーズを切り直す | 要確認 |

---

## 7. 検証戦略

**「4 つの独立した実装が同じ形を出す」ことを常時確認する。**

| # | 実装 | 場所 | 役割 |
|---|---|---|---|
| 1 | Java `SceneSdf` / `CsgBsp` | Grasp3D（凍結） | **移植の正解** |
| 2 | C++ CPU 評価器 | `makina-core` | 計測・テストの基準 |
| 3 | GPU レイマーチ | MitiruEngine | 描画 |
| 4 | POV-Ray | 外部 | パストレースの ground truth |

### 7.1 段階ごとの検証

- **Phase 1**: 1 vs 2（移植が正しいか）
- **Phase 2**: 2 vs 3（CPU と GPU が一致するか）— `Sdf.hpp` 共用により定義上一致するはずだが実測する
- **Phase 5**: 2 vs 3 vs 4（形状の三者比較）
- **Phase 6**: モデラー vs エンジン（同じコア・同じシェーダなので一致するはず）
- **Phase 8**: DX12 vs Vulkan（絵が変わらないことが成果）

### 7.2 比較の軸

- **主軸は形状**: シルエット IoU / 深度差 / 法線角度差
- **シェーディングの厳密一致は目標にしない。** POV-Ray はパストレーサで BRDF もトーンマッピングも異なる。
  同一条件下での傾向確認に留める。

---

## 8. スコープ外（明示的にやらないこと）

- メッシュ抽出（Marching Cubes / Dual Contouring）— D-09
- 有機的・彫刻的モデリング（CSG の不得手領域。ハードサーフェスに絞る）
- Blender / Maya との相互運用
- `SuperEllipsoidBox`（R-06。Phase 0 で最終判断）
- Grasp3D の Swing UI / ヘルプ / i18n / コラボ通信の移植
- Grasp3D 自体の改修（R-10。凍結する）
- Linux / macOS 対応（エンジンが Windows 専用）
- 独自 Undo/Redo（エンジンの巻き戻しに委譲）
- Blender / Maya の操作系のうち §5 Phase 3.D に列挙したもの（モーダルキーマップ全体系、
  マーキングメニュー、マルチビューポート、ドッキング自由化、パネル完全再現、シェルフ編集）

---

## 9. 未決事項

| # | 内容 | 誰が決める | いつまで |
|---|---|---|---|
| **Q-01** | **就活のスケジュール。** いつまでに何が要るか。これによってフェーズの切り方と MVP ラインが変わる | 本人 | **着手前** |
| **Q-02** | MitiruEngine の Vulkan バックエンドは実動作するか（3,064 行あるが未検証） | 本人 | Phase 8 前 |
| **Q-03** | 座標系（R-01） | 調査 | Phase 0 |
| **Q-04** | ノード ID の不変性（R-07） | 設計 | Phase 0 |
| **Q-05** | `SuperEllipsoidBox` を落とすか（R-06） | 設計 | Phase 0 |
| **Q-06** | `makina-core` を独立リポジトリにするか、Makina の submodule にするか | 本人 | Phase 0 |
| **Q-07** | ~~ノード上限 256 は妥当か~~ → ✅ **妥当**。コード生成で 100 ノード / AO / 720p = 15.73 ms（63 fps）。Phase 4 のフィールド追加後に再確認 | 測定済 | 完了 |
| **Q-08** | 既定キーマップを Maya 系とする判断でよいか（D-10）。本人が普段 Blender を使っているなら再考の余地あり | 本人 | Phase 3 前 |
| **Q-09** | `MitiruEngine/docs/INPUT_REBIND_RECIPE.md` のリバインド機構を Makina のキーマップに流用できるか | 調査 | Phase 3 |
| **Q-10** | Phase 3 完了条件の「Maya / Blender 経験者に触ってもらう」の相手を確保できるか（バイト先 / 知人） | 本人 | Phase 3 |

---

## 10. 工数まとめ

| Phase | 内容 | 想定 | 累計 | MVP |
|---|---|---|---|---|
| S | 技術検証スパイク | 2〜4 日 | 〜0.5 週 | ● |
| 0 | 型設計・シーン記述 | 1〜2 週 | 〜2.5 週 | ● |
| 1 | makina-core | 3〜5 週 | 〜7.5 週 | ● |
| 2 | SDF レイマーチ（DX12） | 3〜4 週 | 〜11.5 週 | ● |
| 3 | Makina アプリ + 操作性 | **6〜9 週** | 〜20.5 週 | ● |
| 4 | 経年変化【D1/D2】 | 4〜6 週 | **〜26.5 週** | **● MVP 完成** |
| 5 | クロスチェック【D5】 | 1〜2 週 | 〜28.5 週 | |
| 6 | エンジン統合【D3】 | 2〜3 週 | 〜31.5 週 | |
| 7 | AI 編集【D4】 | 1〜2 週 | 〜33.5 週 | |
| 8 | Vulkan 引き上げ | 3〜4 週 | 〜37.5 週 | |

> **前提**: 週 15〜20 時間程度（アルバイト・学業と並行）。フルタイムなら 1/3 程度。
> **MVP（Phase 4 まで）で約 7 ヶ月。** Q-01 の回答次第で切り直す。

### 10.1 締切が足りない場合の圧縮順序

Q-01 の回答次第で、以下の順に削る（**上から削る**）。

1. Phase 8（Vulkan）を落とす → 約 4 週短縮
2. Phase 7（AI 編集）を落とす → 約 2 週短縮。※ただしテーマの一つなので**最後まで残したい**
3. Phase 6（エンジン統合）を落とす → 約 3 週短縮。姉妹プロジェクトの主張は弱まる
4. Phase 3.C（選択・アウトライナ）を最小実装に → 約 2 週短縮
5. Phase 5（クロスチェック）を落とす → 約 2 週短縮。※安い割に効くので**優先度は低い**
6. **Phase 3.A / 3.B と Phase 4 は削らない。** ここを削ると D1 / D2 が成立せず、
   プロジェクト全体が「未完成」になる
